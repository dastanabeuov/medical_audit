# Руководство по извлечению и оценке ключевых полей консультативных листов

## Обзор

Система автоматического извлечения и оценки ключевых полей из консультативных листов включает три основных компонента:

1. **AdvisorySheetFieldExtractionService** - парсинг и извлечение полей из текста
2. **AdvisorySheetScoringService** - оценка качества заполнения полей
3. **Интеграция с процессом верификации** - автоматическое извлечение при проверке листов

## Архитектура

### 1. Извлечение полей (AdvisorySheetFieldExtractionService)

**Расположение:** `app/services/advisory_sheet_field_extraction_service.rb`

**Извлекаемые поля:**
- Жалобы (complaints)
- Anamnesis morbi (история заболевания)
- Anamnesis vitae (история жизни)
- Объективный осмотр (physical_examination)
- Протокол исследования (study_protocol)
- Диагнозы (diagnoses) с кодом МКБ, основным заболеванием и видом диагноза
- Направления (referrals)
- Назначения (prescriptions)
- Рекомендации (recommendations)

**Принцип работы:**
- Использует регулярные выражения для поиска разделов
- Поддерживает множественные варианты заголовков (русский/латынь)
- Автоматическая нормализация и очистка текста
- Без использования AI - быстро и надежно

**Пример использования:**

```ruby
content = "Жалобы: головная боль\nAnamnesis morbi: болен 3 дня..."
fields = AdvisorySheetFieldExtractionService.extract(content)

# => {
#   complaints: "головная боль",
#   anamnesis_morbi: "болен 3 дня...",
#   ...
# }
```

### 2. Оценка качества (AdvisorySheetScoringService)

**Расположение:** `app/services/advisory_sheet_scoring_service.rb`

**Система оценки:**
- **1.0 балл** - поле заполнено правильно, достаточное количество информации (≥50 символов)
- **0.5 балла** - поле заполнено частично или кратко (10-49 символов)
- **0.0 баллов** - поле не заполнено или критично пустое (<10 символов)

**Методы:**

```ruby
# Оценить один лист
score = AdvisorySheetScoringService.score_sheet(verified_sheet)
# => #<AdvisorySheetScore total_score: 7.5, percentage: 83.33>

# Массовая оценка
results = AdvisorySheetScoringService.score_all
# => { success: 150, failed: 5 }

# Пересчет существующих оценок
results = AdvisorySheetScoringService.recalculate_all_scores
# => { recalculated: 145, errors: 10 }

# Статистика по качеству
stats = AdvisorySheetScoringService.quality_statistics
# => {
#   total: 200,
#   average_score: 7.2,
#   average_percentage: 80.0,
#   by_quality: { excellent: 50, good: 80, ... }
# }

# Листы с низким качеством
low_quality = AdvisorySheetScoringService.low_quality_sheets(limit: 20)

# Листы с высоким качеством
high_quality = AdvisorySheetScoringService.high_quality_sheets(limit: 20)
```

### 3. Модели данных

#### AdvisorySheetField

**Расположение:** `app/models/advisory_sheet_field.rb`

**Связи:**
- `belongs_to :verified_advisory_sheet`

**Полезные методы:**
```ruby
field = verified_sheet.advisory_sheet_field

# Проверить, заполнено ли конкретное поле
field.field_filled?(field.complaints)  # => true/false

# Получить список заполненных полей
field.filled_fields  # => { complaints: "...", diagnoses: {...} }

# Количество заполненных полей
field.filled_fields_count  # => 7

# Процент заполненности
field.completeness_percentage  # => 77.78
```

#### AdvisorySheetScore

**Расположение:** `app/models/advisory_sheet_score.rb`

**Связи:**
- `belongs_to :verified_advisory_sheet`

**Методы:**
```ruby
score = verified_sheet.advisory_sheet_score

# Пересчитать оценку на основе полей
score.calculate_from_fields!

# Получить цвет для визуализации
score.percentage_color  # => "green", "blue", "yellow", "red"

# Получить текстовую оценку
score.quality_label  # => "Хорошее качество"

# Оценить конкретное поле (используется внутренне)
score.evaluate_field(fields, "complaints")  # => 0.0, 0.5, или 1.0
```

## Интеграция с процессом верификации

При верификации консультативного листа автоматически происходит:

1. Парсинг текста и извлечение ключевых полей
2. Сохранение в таблицу `advisory_sheet_fields`
3. Автоматическая оценка качества заполнения
4. Сохранение оценки в таблицу `advisory_sheet_scores`

**Код интеграции** (`app/services/advisory_sheet_verification_service.rb:139-165`):

```ruby
def extract_and_save_fields(verified_sheet, content)
  # 1. Парсинг полей
  fields_data = AdvisorySheetFieldExtractionService.extract(content)

  # 2. Сохранение полей
  verified_sheet.create_advisory_sheet_field!(fields_data)

  # 3. Автоматическая оценка
  AdvisorySheetScoringService.score_sheet(verified_sheet)
end
```

## Отображение в интерфейсе

### Страница просмотра листа (show.html.erb)

**Блок "Ключевые поля"** (`show.html.erb:28-192`):
- Отображает все извлеченные поля с визуальными индикаторами
- Показывает прогресс-бар заполненности
- Отображает оценку качества (если доступна)
- Детальная разбивка баллов по каждому полю

**Цветовая индикация оценок:**
- 🟢 Зеленый (1.0) - поле заполнено полностью
- 🟡 Желтый (0.5) - поле заполнено частично
- 🔴 Красный (0.0) - поле не заполнено

## Тестирование

### Тестовый скрипт

**Расположение:** `tmp/test_field_extraction.rb`

```bash
# Запуск теста извлечения полей
rails runner tmp/test_field_extraction.rb
```

### Тестовые данные

**Расположение:** `tmp/test_advisory_sheet.txt`

Содержит пример консультативного листа со всеми разделами для проверки парсера.

## Примеры использования

### Массовая обработка существующих листов

```ruby
# В Rails консоли
VerifiedAdvisorySheet.where(advisory_sheet_field: nil).find_each do |sheet|
  # Извлекаем поля из body
  fields_data = AdvisorySheetFieldExtractionService.extract(sheet.body)

  # Создаем запись полей
  sheet.create_advisory_sheet_field!(fields_data)

  # Создаем оценку
  AdvisorySheetScoringService.score_sheet(sheet)
end
```

### Получение статистики по качеству

```ruby
stats = AdvisorySheetScoringService.quality_statistics

puts "Всего листов: #{stats[:total]}"
puts "Средний балл: #{stats[:average_score]}"
puts "Средний процент: #{stats[:average_percentage]}%"
puts "Отличное качество (90-100%): #{stats[:by_quality][:excellent]}"
puts "Хорошее качество (80-89%): #{stats[:by_quality][:good]}"
puts "Удовлетворительное (60-79%): #{stats[:by_quality][:satisfactory]}"
puts "Требует улучшения (30-59%): #{stats[:by_quality][:needs_improvement]}"
puts "Критично низкое (<30%): #{stats[:by_quality][:critical]}"
```

### Экспорт данных для отчетности

```ruby
# Получить данные для экспорта в CSV/XLSX
VerifiedAdvisorySheet
  .includes(:advisory_sheet_field, :advisory_sheet_score)
  .find_each do |sheet|
    fields = sheet.advisory_sheet_field
    score = sheet.advisory_sheet_score

    # Формируем строку для экспорта
    row = {
      recording: sheet.recording,
      complaints_score: score&.complaints_score || 0,
      anamnesis_morbi_score: score&.anamnesis_morbi_score || 0,
      # ... остальные поля
      total_score: score&.total_score || 0,
      percentage: score&.percentage || 0,
      quality: score&.quality_label || "Не оценено"
    }

    # Экспорт row в CSV/XLSX
  end
```

## Настройка паттернов парсинга

Для добавления новых вариантов заголовков редактируйте `FIELD_PATTERNS` в `AdvisorySheetFieldExtractionService`:

```ruby
FIELD_PATTERNS = {
  complaints: [
    /жалобы[:\s]+(.*?)(?=anamnesis|...)/mi,
    /новый_паттерн[:\s]+(.*?)(?=...)/mi  # Добавить здесь
  ],
  # ...
}
```

## Будущие улучшения

1. **Экспорт отчетов** - CSV/XLSX с детализацией по всем листам
2. **Dashboard аналитики** - графики и диаграммы по качеству заполнения
3. **Настраиваемые критерии оценки** - гибкая система взвешивания полей
4. **Рекомендации по улучшению** - автоматические подсказки для врачей
5. **Интеграция с Google Sheets** - автоматический экспорт в формат вашей таблицы

## Производительность

- **Парсинг одного листа:** ~10-50ms (без AI)
- **Оценка качества:** ~5-20ms
- **Массовая обработка 1000 листов:** ~20-60 секунд

## Требования

- PostgreSQL с поддержкой JSONB (для поля diagnoses)
- Rails 8.0+
- Ruby 3.x+
