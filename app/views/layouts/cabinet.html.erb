<!DOCTYPE html>
<html lang="ru">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Кабинет - Медицинский аудит КЛ</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>
  <body class="bg-gray-100 min-h-screen">
    <nav class="bg-white shadow-sm">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex items-center">
            <a href="/" class="text-xl font-bold text-blue-600">МедАудит КЛ</a>
          </div>
          <div class="flex items-center space-x-4">
            <% if current_auditor %>
              <span class="text-gray-600">Аудитор: <%= current_auditor.full_name %></span>
              <%= link_to "Выход", destroy_auditor_session_path, method: :delete, data: { turbo_method: :delete }, class: "text-red-600 hover:text-red-800" %>
            <% elsif current_main_doctor %>
              <span class="text-gray-600">Главный врач: <%= current_main_doctor.full_name %></span>
              <%= link_to "Выход", destroy_main_doctor_session_path, method: :delete, data: { turbo_method: :delete }, class: "text-red-600 hover:text-red-800" %>
            <% elsif current_doctor %>
              <span class="text-gray-600">Врач: <%= current_doctor.full_name %></span>
              <%= link_to "Выход", destroy_doctor_session_path, method: :delete, data: { turbo_method: :delete }, class: "text-red-600 hover:text-red-800" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
      <% if notice %>
        <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-4" role="alert">
          <%= notice %>
        </div>
      <% end %>
      <% if alert %>
        <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-4" role="alert">
          <%= alert %>
        </div>
      <% end %>
      <%= yield %>
    </main>
  </body>
</html>
