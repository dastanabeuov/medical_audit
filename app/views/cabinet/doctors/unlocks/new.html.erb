<div class="min-h-screen flex items-center justify-center bg-gradient-to-br from-gray-50 to-gray-100 px-4 sm:px-6 lg:px-8">
  <div class="max-w-md w-full space-y-8">
    <div class="bg-white rounded-2xl shadow-xl p-8">
      <div class="text-center mb-8">
        <div class="<%= icon_bg_class(:doctor) %> w-20 h-20 rounded-full flex items-center justify-center mx-auto mb-4 shadow-lg">
          <%= scope_icon_svg(:doctor) %>
        </div>
        <h2 class="text-3xl font-bold text-gray-900 mb-2">
          <%= t('devise.unlocks.new.title', default: 'Разблокировка аккаунта') %>
        </h2>
        <p class="text-sm text-gray-600">
          <%= t('devise.unlocks.new.subtitle', default: 'Введите ваш email для получения инструкций по разблокировке') %>
        </p>
      </div>

      <%= form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post, class: "space-y-6" }) do |f| %>
        <%= render "cabinet/doctors/shared/error_messages", resource: resource %>

        <div>
          <%= f.label :email, t('views.devise.sessions.email', default: 'Email'), class: "block text-sm font-semibold text-gray-700 mb-2" %>
          <%= f.email_field :email, autofocus: true, autocomplete: "email",
              class: "appearance-none block w-full px-4 py-3 border border-gray-300 rounded-lg placeholder-gray-400 #{input_focus_class(:doctor)} transition-colors duration-200 sm:text-sm",
              placeholder: "name@example.com" %>
        </div>

        <div>
          <%= f.submit t('devise.unlocks.new.submit', default: 'Отправить инструкции'), class: button_class(:doctor) %>
        </div>
      <% end %>

      <%= render "cabinet/doctors/shared/links" %>
    </div>

    <p class="text-center text-xs text-gray-500 mt-4">
      <%= t('views.devise.footer', default: '© 2025 Medical Audit System') %>
    </p>
  </div>
</div>
