<div class="space-y-6">
  <h1 class="text-2xl font-bold text-gray-900">Личный кабинет главного врача</h1>

  <div class="bg-white rounded-lg shadow p-6">
    <h2 class="text-lg font-semibold text-gray-900 mb-4">Подчиненные врачи</h2>
    <% if @doctors.any? %>
      <ul class="divide-y divide-gray-200">
        <% @doctors.each do |doctor| %>
          <li class="py-3 flex justify-between items-center">
            <span class="text-gray-900"><%= doctor.full_name %></span>
            <span class="text-gray-500 text-sm"><%= doctor.specialization %></span>
          </li>
        <% end %>
      </ul>
    <% else %>
      <p class="text-gray-500">Нет подчиненных врачей</p>
    <% end %>
  </div>

  <div class="bg-blue-50 rounded-lg shadow p-6 border-l-4 border-blue-500">
    <h2 class="text-lg font-semibold text-blue-800 mb-2">Уведомления</h2>
    <p class="text-blue-700">Функционал уведомлений будет добавлен в следующей версии.</p>
  </div>
</div>
