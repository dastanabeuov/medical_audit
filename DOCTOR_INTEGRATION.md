# Интеграция автоматического создания аккаунтов врачей

## Обзор

Система автоматически извлекает данные врачей из консультативных листов (КЛ), ищет их на сайте medelement.com, создает учетные записи и привязывает КЛ к врачам.

## Компоненты

### 1. DoctorDataExtractorService
**Файл:** `app/services/doctor_data_extractor_service.rb`

Извлекает ФИО врача из текста консультативного листа.

**Паттерны поиска:**
- "Врач: Иванов Иван Иванович"
- "Доктор: Петров Петр"
- "Лечащий врач: Сидорова Мария Петровна"

**Возвращаемые данные:**
```ruby
{
  full_name: "Иванов Иван Иванович",
  first_name: "Иван",
  last_name: "Иванов",
  second_name: "Иванович"
}
```

### 2. MedelementScraperService
**Файл:** `app/services/medelement_scraper_service.rb`

Парсит данные врачей с сайта medelement.com.

**Функции:**
- Авторизация на сайте через ENV переменные
- Поиск врача по ФИО в нескольких клиниках
- Извлечение email, специализации, отделения

**Используемые клиники:**
```ruby
CLINIC_URLS = [
  "https://co.medelement.com/ref_companies/view/NTg4ODUxNjAxNjU3MTc1MjU3/fDh8",
  "https://co.medelement.com/ref_companies/view/NTIwODU4NTkxNzE3MDczODQz/fDh8",
  "https://co.medelement.com/ref_companies/view/MTYzMTk3MDE2MzA5MjMyMTg%253D/fDh8"
]
```

**Возвращаемые данные:**
```ruby
{
  email: "doctor@clinic.kz",
  first_name: "Иван",
  last_name: "Иванов",
  second_name: "Иванович",
  specialization: "Терапевт",
  department: "Поликлиника №1",
  clinic: "ГКП на ПХВ Поликлиника №1",
  found_on_medelement: true
}
```

### 3. DoctorAccountService
**Файл:** `app/services/doctor_account_service.rb`

Управляет созданием и поиском аккаунтов врачей.

**Основные методы:**
- `find_or_create(doctor_data)` - находит или создает врача
- `link_to_advisory_sheet(doctor, verified_sheet)` - привязывает КЛ к врачу

**Пароль по умолчанию:** `Qq123456!`

**Автогенерация email:**
Если email не найден на medelement.com, генерируется email вида:
```
doctor-1@emirmed.kz
doctor-2@emirmed.kz
doctor-3@emirmed.kz
...
```

### 4. Интеграция в процесс верификации
**Файл:** `app/services/advisory_sheet_verification_service.rb`

После верификации КЛ автоматически:
1. Извлекается ФИО врача из текста
2. Выполняется поиск на medelement.com
3. Создается или находится аккаунт врача
4. КЛ привязывается к врачу через таблицу `relationships_doctor_and_verified_advisory_sheets`

## Настройка

### 1. Переменные окружения

Добавьте в `.env` файл:

```bash
# Medelement.com Integration
MEDELEMENT_LOGIN=d.abeuov@emirmed.kz
MEDELEMENT_PASSWORD=Qq123456!
```

### 2. Миграции

Все необходимые таблицы уже существуют:
- `doctors` - таблица врачей
- `relationships_doctor_and_verified_advisory_sheets` - связь врачей и КЛ

### 3. Добавление новых клиник

Чтобы добавить новую клинику для поиска врачей, отредактируйте:

**Файл:** `app/services/medelement_scraper_service.rb`

```ruby
CLINIC_URLS = [
  "https://co.medelement.com/ref_companies/view/...",
  "https://co.medelement.com/ref_companies/view/...",
  # Добавьте новую ссылку здесь
  "https://co.medelement.com/ref_companies/view/НОВАЯ_ССЫЛКА/fDh8"
]
```

## Использование

### Автоматическая обработка

При загрузке и верификации КЛ система автоматически:
1. Извлекает ФИО врача
2. Ищет врача на medelement.com
3. Создает аккаунт (если не существует)
4. Привязывает КЛ к врачу

### Просмотр списка врачей

**URL:** `/cabinet/auditors/doctors`

**Навигация:** Кабинет Аудитора → Врачи

**Отображаемая информация:**
- ФИО врача
- Email (логин)
- Пароль (одинаковый для всех: `Qq123456!`)
- Отделение
- Специализация
- Клиника
- Количество привязанных КЛ
- Дата создания аккаунта

**Метки:**
- 🟢 **Из medelement.com** - данные получены с сайта
- 🟡 **Автогенерированный** - email сгенерирован автоматически

## Логирование

Все действия логируются в Rails logger:

```ruby
# Успешное извлечение ФИО
"Извлечено ФИО врача из КЛ: Иванов Иван Иванович"

# Найден на medelement.com
"Врач найден на medelement.com: doctor@clinic.kz"

# Не найден на сайте
"Врач не найден на medelement.com, будет создан с автогенерированным email"

# Создан новый аккаунт
"Created doctor: Иванов Иван Иванович (doctor-1@emirmed.kz)"

# Привязан к КЛ
"КЛ #123456 привязан к врачу: Иванов Иван Иванович (doctor@clinic.kz)"
```

## Обработка ошибок

Система спроектирована так, чтобы ошибки при работе с врачами **не прерывали** процесс верификации КЛ:

- Если не удалось извлечь ФИО → КЛ будет обработан без привязки к врачу
- Если не удалось подключиться к medelement.com → будет создан аккаунт с автогенерированным email
- Если не удалось создать аккаунт → ошибка логируется, но КЛ остается проверенным

## Структура БД

### Таблица doctors

```sql
CREATE TABLE doctors (
  id SERIAL PRIMARY KEY,
  first_name VARCHAR NOT NULL,
  second_name VARCHAR,
  last_name VARCHAR NOT NULL,
  email VARCHAR NOT NULL UNIQUE,
  department VARCHAR NOT NULL,
  specialization VARCHAR NOT NULL,
  clinic VARCHAR NOT NULL,
  date_of_employment TIMESTAMP NOT NULL,
  doctor_identifier VARCHAR,
  main_doctor_id BIGINT,
  -- Devise fields (password, confirmation, etc.)
  encrypted_password VARCHAR NOT NULL DEFAULT '',
  ...
  created_at TIMESTAMP NOT NULL,
  updated_at TIMESTAMP NOT NULL
);
```

### Таблица relationships

```sql
CREATE TABLE relationships_doctor_and_verified_advisory_sheets (
  id SERIAL PRIMARY KEY,
  verified_advisory_sheet_id BIGINT NOT NULL,
  doctor_id BIGINT NOT NULL,
  created_at TIMESTAMP NOT NULL,
  updated_at TIMESTAMP NOT NULL,
  FOREIGN KEY (verified_advisory_sheet_id) REFERENCES verified_advisory_sheets(id),
  FOREIGN KEY (doctor_id) REFERENCES doctors(id)
);
```

## Тестирование

### Ручное тестирование

1. Загрузите КЛ с указанием врача:
   ```
   Врач: Иванов Иван Иванович
   ```

2. Дождитесь верификации КЛ

3. Проверьте список врачей: `/cabinet/auditors/doctors`

4. Убедитесь, что:
   - Врач создан
   - Email либо с medelement.com, либо автогенерированный
   - КЛ привязан к врачу (показано количество)

### Проверка логов

```bash
tail -f log/development.log | grep -i doctor
```

## Известные ограничения

1. **HTML структура medelement.com** - если сайт изменит структуру, потребуется обновление селекторов в `MedelementScraperService`

2. **Авторизация** - использует форму входа с CSRF токеном, может потребовать обновления при изменении на сайте

3. **Поиск врача** - использует точное совпадение ФИО, может не найти врача при опечатках

4. **Семантическое сопоставление полей** - в текущей версии не реализовано через AI, используется прямое сопоставление

## Будущие улучшения

- [ ] Добавить AI-powered семантическое сопоставление полей (DoctorFieldMapperService с Gemini)
- [ ] Кэширование результатов парсинга medelement.com
- [ ] Уведомления врачам по email при привязке КЛ
- [ ] Bulk-импорт врачей из Excel/CSV
- [ ] История изменений данных врача
